<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>LiveCommentOverlay</RootNamespace>
    <AssemblyName>LiveCommentOverlay</AssemblyName>
    <!-- No launchSettings / appsettings on purpose: this mirrors the zero-config Node server. -->
  </PropertyGroup>

</Project>
