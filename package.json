{
  "name": "live-youtube-comments-overlay",
  "version": "0.1.0",
  "private": true,
  "description": "Local YouTube live chat comment overlay for OBS.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
